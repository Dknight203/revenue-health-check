# Optional: Webhook URL for lead capture
# If not set, the app works without webhook and stores data locally only
VITE_WEBHOOK_URL=https://your-webhook-endpoint.com/api/lead

# Google Gemini API Key (hardcoded in geminiAnalysis.ts for now)
# VITE_GEMINI_API_KEY=your_api_key_here
